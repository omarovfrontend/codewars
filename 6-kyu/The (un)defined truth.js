/*
DESCRIPTION:
You'll have to make a function output return the word defined but without using:

void 0, undefined, var, let, const, regex and eval
String, Boolean, Number, Array, Object, Function
Literals for strings (single, double and backticks), objects, arrays, numbers and operators +, -, /, * (means no JsF*ck you)
((un)?defined) anywhere

#PUZZLES #GAMES
*/

function output() {
  return (typeof $).slice(Math.E);
}

